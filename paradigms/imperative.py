# Uses the imperative mood of natural language to express directions.
# It executes commands in step-by-step manner

sample_characters = ['p','y','t','h','o','n']
sample_string = ''
sample_string
for c in sample_characters:
   sample_string = sample_string + c
   print(sample_string)