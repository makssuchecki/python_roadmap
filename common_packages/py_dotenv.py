from dotenv import load_dotenv
# python-dotenv reads key-value pairs from a .env file and can set them as environment variables.

load_dotenv() # reads variables from a .env file ands sets them in os.environ